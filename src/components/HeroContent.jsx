import '../styles/partials/_hero.scss';
import Nav from './nav';
const HeroSection = () => {
  return (
    <header className='header'>
      <Nav />
    </header>
  );
};

export default HeroSection;
